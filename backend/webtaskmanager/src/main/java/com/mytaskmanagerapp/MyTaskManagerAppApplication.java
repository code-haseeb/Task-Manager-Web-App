// MyTaskManagerAppApplication.java

package com.mytaskmanagerapp; // Update the package declaration to match the actual folder structure

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MyTaskManagerAppApplication {
    public static void main(String[] args) {
        SpringApplication.run(MyTaskManagerAppApplication.class, args);
    }
}
